{
	"name": "@taylux/core",
	"version": "0.0.1",
	"main": "src/index.ts",
	"type": "module",
	"types": "src/index.ts",
	"private": false,
	"scripts": {
		"build": "vite build",
		"lint": "eslint \"src/**/*.ts\" --fix",
		"typecheck": "tsc --noEmit",
		"clean": "rm -rf dist .turbo"
	},
	"publishConfig": {
		"access": "public"
	}
}
